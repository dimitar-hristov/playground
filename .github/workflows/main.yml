on:
  issues:
    types:
      - opened

jobs:
  sayHello:
    runs-on: ubuntu-latest
    steps:
      - uses: dimitar-hristov/foo-probot@v1.2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
